// @flow

export type Item = {
    id: number,
    name: string,
    price: number,
    delimiter: number,
    created: Date,
    updated: Date
}

export type User = {
    id: number,
    name: string
}

